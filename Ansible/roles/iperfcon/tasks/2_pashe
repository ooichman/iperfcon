- name: get the route for iperf client
  k8s_info:
    kind: Route
    name: iperf-client
  register: iperf-client-route

- name: deploying the iperf-check
  k8s:
    state: "{{ state }}"
    definition: "{{ lookup('template', 'iperf-check-deployment.yml.j2') | from_yaml }}"
